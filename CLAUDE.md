@AGENTS.md

## テスト方針(プロジェクトルール)

- **単体テスト(Vitest, `src/**/*.test.ts`)**: API/ビジネスロジック層(CSVアダプター、
  同期ロジック、集計・変換関数など)に対象を絞る。UIコンポーネントの単体テストは書かない。
  - Given-When-Then のシナリオベースで書く(`it("Given ..., When ..., Then ...")`
    + 本体を `// Given` `// When` `// Then` で区切る)。テスト自体が仕様書として
    読めることを目指す。
  - 正常系だけでなく、実際のCSV会社ごとのクセ(ヘッダー行・合計行・内訳行の除外、
    返金、海外通貨、除外すべき取引種別など)を1シナリオ1ケースで網羅する。
- **結合テスト**: IndexedDB(Dexie, `fake-indexeddb`使用)⇔Supabase同期など、
  複数モジュールを跨ぐ処理を対象にする。
- **E2E(Playwright, `e2e/`)**: 主要シナリオのみを補足的にカバーする
  (網羅性はユニットテスト側に持たせる)。
- **実装完了の条件**: `pnpm test` と `pnpm test:e2e` の主要シナリオが全てパスしていること。
  新しいビジネスロジックを追加/変更したら、対応するユニットテストを必ず追加/更新する。
